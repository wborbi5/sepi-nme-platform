import ResourceStub from "@/components/ResourceStub";

export const metadata = { title: "Mentor Directory — SEPi Portal" };

export default function MentorDirectoryPage() {
  return (
    <ResourceStub
      title="Mentor Directory"
      blurb="Alumni and operators willing to take a meeting. Content coming soon."
    />
  );
}
