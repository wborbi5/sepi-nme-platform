import ResourceStub from "@/components/ResourceStub";

export const metadata = { title: "Tools — SEPi Portal" };

export default function ToolsPage() {
  return (
    <ResourceStub
      title="Tools"
      blurb="Software, credits, and templates the chapter has access to. Content coming soon."
    />
  );
}
