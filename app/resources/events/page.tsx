import ResourceStub from "@/components/ResourceStub";

export const metadata = { title: "Events — SEPi Portal" };

export default function EventsPage() {
  return (
    <ResourceStub
      title="Events"
      blurb="Sessions, speakers, and Athena 42. Content coming soon."
    />
  );
}
